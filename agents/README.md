# Agents

This repository orchestrates several subagents for specific styling or architectural tasks. 
Currently supported platforms and subagents can dynamically tap into `agency-designer-skill`.

## Integration Instructions

1. Configure your AI agent workspace to source local instructions from `AGENCY_DESIGNER.md`.
2. Allow your Subagents to call `/design` or `/animate` commands within their workflow runs.
